services:
  db:
    environment:
      - MARIADB_ROOT_PASSWORD=${DB_ROOT_PASSWORD:-symfony}
      - MARIADB_DATABASE=${DB_DATABASE:-symfony}
      - MARIADB_USER=${DB_USER:-symfony}
      - MARIADB_PASSWORD=${DB_PASSWORD:-symfony}
